import type { MapPoint } from "../types";

export const POINTS: MapPoint[] = [
  // Ajoutez vos points ici après les avoir placés via le mode debug (?debug=true)
  // Exemple :
  // { id: "point-1", name: "Entrée principale", x: 0.5, y: 0.3, zoom: 2 },
];
